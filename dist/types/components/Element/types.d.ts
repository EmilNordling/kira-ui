import { BaseElementComponent } from '../BaseElement/BaseElement.types';
export declare namespace ElementComponent {
    interface Props extends Style, BaseElementComponent.Props {
    }
    interface Style {
    }
}
