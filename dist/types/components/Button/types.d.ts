import { BaseButtonComponent } from '../BaseButton/BaseButton.types';
export declare namespace ButtonComponent {
    interface Props extends Style, BaseButtonComponent.Props {
    }
    interface Style {
    }
}
